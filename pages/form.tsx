import type { NextPage } from 'next';
import Head from 'next/head';
import { Body } from '../components/Body';

const Form: NextPage = () => {
    return (
        <>
            <Head>
                <title>דנה כתר</title>
            </Head>
            <Body />
        </>
    );
};

export default Form;
